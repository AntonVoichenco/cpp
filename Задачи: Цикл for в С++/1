/*Написать программу, которая будет показывать на экран квадрат числа,
введенного пользователем. Пользователь должен сам решать –  выйти из программы или продолжить ввод.
(Подсказка – необходимо запустить бесконечный цикл, в котором предусмотреть его прерывание,
при наступлении определённого условия).*/
#include<iostream>
using namespace std;
int main() {
	setlocale(LC_ALL, "ru");
	for (;;) {
	int numberPrt = 0;
	cout << "Введите сисло которое хотите возвести в квадрат или для прекращения введите 0 : "; cin >> numberPrt;
	if (numberPrt == 0)
		break;
	cout<<numberPrt<<"^2 = "<< numberPrt * numberPrt;
	}
	system("pause");
}
